/**
 * 
 * @author rsb
 */
'use strict';

// cluster client
const clusterClient = require('./cluster-client').getClient();

const newChatRoom = (chatroom) => {
    
}

module.exports = {
    newChatRoom
}